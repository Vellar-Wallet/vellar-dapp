# Batch Transaction Status Poll Route Module

Mock route module simulating a batch transaction status poll.

## Usage

```javascript
const poll = require('./index');
const results = poll(['0x123', '0x456']);
console.log(results);
```
