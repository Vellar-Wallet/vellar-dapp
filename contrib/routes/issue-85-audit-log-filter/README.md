# Audit Log Filter Route Module

Mock route module for audit log filtering by actor and action.

## Usage

```javascript
const filter = require('./index');
const results = filter('user1', 'login');
console.log(results);
```
