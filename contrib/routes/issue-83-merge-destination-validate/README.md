# Merge Destination Validate Route Module

Mock route module for account merge destination validation.

## Usage

```javascript
const validate = require('./index');
const results = validate('account1', 'account2');
console.log(results);
```
