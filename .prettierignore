# Source-of-truth docs — never reformat (see CLAUDE.md)
idea.md
technical-doc.md

pnpm-lock.yaml
.next/
.output/
.wxt/
.turbo/
dist/

# Generated / tool-owned files — don't reformat (their format is defined by the
# tool that writes them, and reformatting can break the tooling that reads them)
contracts/**/test_snapshots/
**/drizzle/meta/
target/
